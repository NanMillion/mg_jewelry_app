name: mg_jewelry
description: Gold Jewelry Management App
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # 🔥 BACKEND
  supabase_flutter: ^2.5.0

  # 📦 STATE MANAGEMENT
  provider: ^6.1.2

  # 🎨 UI / DESIGN
  google_fonts: ^6.2.1
  
  flutter_animate: ^4.5.0

  glassmorphism: ^3.0.0

  url_launcher: ^6.2.5

  lottie: ^3.1.0

  shimmer: ^3.0.0
  
  intl: ^0.19.0

  # 📊 CHARTS (DASHBOARD)
  fl_chart: ^0.68.0

  # 📡 NETWORK
  http: ^1.6.0
  
  pdf: ^3.10.8

  hive: ^2.2.3
  
  hive_flutter: ^1.1.0

  web: ^0.5.0

  file_picker: ^8.0.0

  firebase_core: ^3.0.0

  firebase_messaging: ^15.0.0

  excel: ^2.1.0

  qr_flutter: ^4.1.0

  mobile_scanner: ^5.1.0

  printing: ^5.12.0

  share_plus: ^7.2.1
  
  path_provider: ^2.1.2
  
  open_filex: ^4.4.0

  # 🔔 NOTIFICATIONS (future)
  flutter_local_notifications: ^17.1.2

  # 📂 FILE / IMAGE
  image_picker: ^1.1.2

  # 📍 GPS
  geolocator: ^12.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true